#define WINDOW_WIDTH  640
#define WINDOW_HEIGHT 480

//以窗口中心为坐标原点
#define CONVERT_X_POS(x) ((WINDOW_WIDTH/2)+(x))
#define CONVERT_Y_POS(y) ((WINDOW_HEIGHT/2)+(y))

#define KEY_END !keystate(key_enter)
